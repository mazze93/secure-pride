# macOS
.DS_Store
**/.DS_Store
Thumbs.db

# Editor / tooling
.vscode/
.idea/
.claude/
*.jam
*.swp
*.swo
*~
.superpowers/

# Environment variables
.env
.env.local
.env.*.local

# Build artefacts & caches
node_modules/
.wrangler/
dist/
.astro/
.cache/

# Reports & generated output
lighthouse-report.json

# Archives — review locally before re-adding
*.zip
*.tar.gz
*.tar.bz2

# Package manager locks (committed per project preference — remove if you pin lockfiles)
yarn.lock
pnpm-lock.yaml

# Sensitive docs — store in a secrets manager or encrypted vault, not git
docs/*.pdf
docs/brand/*.docx

# Staging inbox — clear after importing, never commit
assets/incoming-*/
